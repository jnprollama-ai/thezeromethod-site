---
const navLinks = [
  { href: "/", label: "Home" },
  { href: "/product", label: "Product" },
  { href: "/blog", label: "Blog" }
];
---

<header style="position: sticky; top: 0; z-index: 100; background: rgba(255, 255, 255, 0.95); backdrop-filter: blur(10px); border-bottom: 1px solid var(--border);">
  <div class="container" style="display: flex; align-items: center; justify-content: space-between; height: 72px;">
    <!-- Logo -->
    <a href="/" style="display: flex; align-items: center; gap: 0.5rem; text-decoration: none;">
      <span style="font-family: var(--font-display); font-size: 1.75rem; font-weight: 700; color: var(--navy-900);">0</span>
      <span style="font-family: var(--font-sans); font-size: 1.25rem; font-weight: 600; color: var(--text-primary);">ZeroMethod</span>
    </a>
    
    <!-- Navigation -->
    <nav style="display: flex; align-items: center; gap: 2rem;">
      <div style="display: flex; gap: 2rem;">
        {navLinks.map(link => (
          <a 
            href={link.href} 
            style="font-size: 0.9375rem; color: var(--text-secondary); text-decoration: none; font-weight: 500; transition: color 0.2s;"
            class="nav-link"
          >
            {link.label}
          </a>
        ))}
      </div>
      
      <a 
        href="/product" 
        class="btn btn-primary" 
        style="padding: 0.625rem 1.25rem; font-size: 0.875rem; background: var(--navy-900);"
      >
        Get Started
      </a>
    </nav>
  </div>
</header>

<style>
  .nav-link:hover {
    color: var(--text-primary) !important;
  }
</style>